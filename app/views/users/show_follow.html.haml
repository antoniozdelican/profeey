- provide(:title, @title)
.row
  .col-md-4
    = @users.count
  .col-md-8
    %h3= @title
    - if @users.any?
      %ul.users.follow
        //  render partial: "users/user", collection: @users
        = render @users